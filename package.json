{
  "name": "@anandchowdhary/food",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Public food log data and generated README.",
  "scripts": {
    "export:restaurants": "node scripts/export-restaurants.mjs",
    "generate": "node generate-readme.mjs",
    "format": "prettier --write README.md generate-readme.mjs scripts/export-restaurants.mjs .github/workflows/*.yml package.json .prettierrc.json data/restaurants.json",
    "check": "npm run export:restaurants && npm run generate && npm run format && prettier --check README.md generate-readme.mjs scripts/export-restaurants.mjs .github/workflows/*.yml package.json .prettierrc.json data/restaurants.json"
  },
  "dependencies": {
    "csv-parse": "^6.2.1"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
